# AI News Agent

一套 n8n 自動化系統，每日自動蒐集 AI 相關新聞，透過 LLM（Gemini）進行評分與摘要，經過 Google Sheets 上的人工審核關卡後，於每週固定時間彙整成 HTML 公告並以 Email 寄出。

## 運作原理

兩條 n8n workflow 彼此獨立，只透過共用的 Google Sheet 溝通，不會互相直接呼叫——因此可以各自獨立除錯、排程、或重跑，不會互相影響。

```
Workflow A（每日執行）                      Workflow B（每週執行）
──────────────────                        ───────────────────
多個 RSS 新聞來源                           讀取「待審核」分頁中
   │                                       核可 = TRUE 且 已寄送 = FALSE 的項目
   ▼                                                │
合併來源 + 比對「已處理」歷史紀錄去重                ▼
   │                                       依分數排序，組成 HTML 公告內容
   ▼                                                │
篩選近7天內、依新到舊排序                              ▼
   │                                       透過 SMTP 寄送
   ▼                                                │
限制筆數上限                                          ▼
   │                                       將對應項目標記 已寄送 = TRUE
   ▼                                                │
單次批次呼叫 Gemini                                   ▼
（一次請求評分並摘要所有候選新聞）            清空整個「待審核」分頁
   │                                                │
   ▼                                                ▼
分數達門檻 → 寫入「待審核」                    「已處理」僅保留近14天資料、
所有項目（不論分數）→ 寫入「已處理」            依連結去重後重新寫回
```

- **Workflow A** 每天執行一次，蒐集候選新聞，用單次批次呼叫 LLM 完成評分（不是逐則個別呼叫——這對免費層 API 的速率限制很重要），並依結果分別寫入 Google Sheet 的兩個分頁：`待審核`（分數達門檻、等待人工核可）與 `已處理`（不論分數高低，全部完整記錄，作為歷史紀錄與去重依據）。
- **Workflow B** 每週執行一次。人工需在此之前先到 `待審核` 分頁勾選要發送的項目（`核可`）。此 workflow 會寄出一封彙整信件、標記已寄送的項目，接著清空 `待審核` 供下週使用，並將 `已處理` 修剪成只保留近 14 天的滾動視窗。

## 檔案說明

| 檔案 | 說明 |
|---|---|
| `[AI News Agent] A-蒐集與評分.json` | 每日蒐集、去重、Gemini 評分之 workflow |
| `[AI News Agent] B-審核後寄送.json` | 每週審核彙整、寄信、清理 Sheets 之 workflow |

## 前置需求

- 一個 n8n 環境（Cloud 或自架皆可）
- 一個 Google 帳號，並準備：
  - 一份 Google Sheet，包含 `待審核` 與 `已處理` 兩個分頁（欄位結構見下方）
  - 在 n8n 裡設定好 Google Sheets OAuth2 憑證
- 一組 Gemini API Key（於 [aistudio.google.com/apikey](https://aistudio.google.com/apikey) 申請）
- 一個可用於寄信的 SMTP 帳號

## 設定步驟

### 1. 建立 Google Sheets

建立一份試算表，包含以下兩個分頁，並在第一列填入完全一致的欄位名稱：

**`待審核`**
```
標題 | 摘要 | 分類 | 分數 | 評分理由 | 來源連結 | 發布日期 | 核可 | 已寄送
```

**`已處理`**
```
標題 | 摘要 | 分類 | 分數 | 評分理由 | 來源連結 | 發布日期 | 處理時間
```

`核可` 與 `已寄送` 建議設成核取方塊格式——**只套用在標題列即可**，不要對一大片空白範圍預先套用格式，否則之後新資料可能會被寫入到很後面的列，而不是緊接在現有資料之後。

### 2. 在 n8n 設定憑證

- **Google Sheets OAuth2**：`Credentials → New → Google Sheets OAuth2 API`，用擁有上述試算表的 Google 帳號完成授權。
- **SMTP**：`Credentials → New → SMTP`。若使用 Gmail，需先開啟兩步驟驗證，再申請一組[應用程式密碼](https://myaccount.google.com/apppasswords)——Host 填 `smtp.gmail.com`，Port `587`，開啟 STARTTLS。
- **Gemini API Key**：不需要另外建立 n8n 憑證，是直接以 Header 的方式寫在 HTTP Request 節點裡（見下方 placeholder 對照表）。

### 3. 匯入兩個 workflow

把兩份 JSON 檔案匯入 n8n（可用 `Import from File`，或直接把 JSON 內容貼到空白畫布上）。

### 4. 替換所有 placeholder

兩份檔案裡凡是跟環境相關的資訊，都用 `{PLACEHOLDER}` 這種格式標記，需要逐一搜尋替換，並且**在每個 Google Sheets／SMTP 節點裡重新選一次憑證**（因為佔位用的憑證 ID 對應不到任何真實憑證）：

| Placeholder | 出現位置 | 替換成 |
|---|---|---|
| `{GOOGLE_SPREADSHEETS_LINK}` | 兩份 workflow 裡所有 Google Sheets 節點 | 試算表網址中的 ID（`.../d/`與`/edit`中間那一串） |
| `{GOOGLE_GEMINI_APIKEY}` | Workflow A「Gemini Ratings and 摘要」節點的 Header | 你的 Gemini API Key |
| `{SMTP_SENDER}` | Workflow B 「SMTP」節點 | 寄件信箱地址 |
| `{AD_RECIPIENT}` | Workflow B 「SMTP」節點 | 收件人地址，多個收件人用逗號分隔 |

### 5. 依自己團隊調整評分 Prompt

Prompt 內容寫在 Workflow A 的「組合批次請求」節點裡，目前是針對一般性的 PC／硬體研發團隊設計的——正式使用前，建議把部門背景、關鍵字範例、分類清單都改成符合自己團隊的實際情況，評分結果才會準確。

要留意：這段 Prompt 每次執行都會被送往外部 LLM API，**避免把公司具名、合作夥伴清單等敏感內部資訊直接寫進去**。

### 6. 先測試再啟用

1. 手動執行一次 Workflow A（`Execute workflow`），確認 `待審核`、`已處理` 都有正確寫入資料。
2. 在 `待審核` 分頁勾選一兩筆的 `核可`。
3. 手動執行一次 Workflow B，確認信件有寄出、`已寄送` 有被標記、`待審核` 有被清空、`已處理` 的清理與去重邏輯也正常運作。
4. 確認都沒問題後，把兩個 workflow 切換成 **Active**。

## 資料模型

### `待審核` 分頁

| 欄位 | 說明 |
|---|---|
| 標題 | LLM 生成的精簡標題 |
| 摘要 | 2-3 句話的摘要 |
| 分類 | LLM 判斷的分類標籤 |
| 分數 | 0-10 的整數相關性分數 |
| 評分理由 | 一句話說明評分理由 |
| 來源連結 | 新聞原始連結——同時也是去重／比對的依據欄位 |
| 發布日期 | 新聞原始發布時間 |
| 核可 | 核取方塊，人工核可是否納入本週公告 |
| 已寄送 | 核取方塊，寄送成功後系統自動標記 |

### `已處理` 分頁

前七個欄位跟 `待審核` 相同，少了 `核可`／`已寄送`，多了：

| 欄位 | 說明 |
|---|---|
| 處理時間 | 系統實際執行評分的時間戳記（不同於新聞本身的發布時間） |

`已處理` 會記錄**所有**經過評分的項目，不論分數高低，一方面避免同一則新聞被重複處理，另一方面也讓人工有機會複查是否有被 LLM 低估、值得撈回來的新聞。

## 評分機制

每則新聞由單一批次 LLM 呼叫（所有候選新聞打包成一次請求，而非逐則呼叫，詳見[設計筆記]依以下四個維度給出 0-10 分：

- **internal_relevance**（部門相關性）——與團隊技術棧、工作內容的相關程度
- **efficiency_value**（效率提升價值）——是否有助於提升個人／團隊生產力
- **must_know**（產業重要性）——即使不直接相關，是否仍屬產業重大消息
- **learning_value**（學習價值）——即使跟工作沒有直接關聯，是否仍值得閱讀學習

建議的分數區間：9-10 分（所有人都該讀）、7-8 分（高度相關）、5-6 分（特定職能相關）、3-4 分（相關性有限）、0-2 分（不建議）。目前收錄進 `待審核` 的預設門檻是 7 分。

## 設計筆記

幾個不看程式碼不容易看出來的設計決策，之後要修改時可以參考：

- **批次評分，而非逐則呼叫**：每次執行時，所有候選新聞會打包成單一個 Prompt，一次呼叫 Gemini 完成評分，而不是每則新聞各呼叫一次。這樣可以讓每日 API 用量遠低於免費層的速率限制。
- **模型使用別名，不釘死版本號**：Gemini 模型用的是 `-latest` 這種別名，而不是特定日期版本，因為 Google 會不定期讓特定版本的模型下架、不再開放新用戶使用。
- **去重機制依賴 `已處理`，不是 `待審核`**：因為 `待審核` 每週都會被完全清空，長期的去重歷史全部仰賴 `已處理`。14 天的保留期刻意設得比 7 天的新聞蒐集範圍更寬，多留一週緩衝空間。
- **RSS 來源本身不一定有時間限制**：部分 RSS 來源會回傳全站歷史文章，而不是只回傳近期項目，所以 workflow 自己另外做了一層依發布日期的篩選，不能完全仰賴來源本身。

## 目前限制

- 當週若沒有任何項目被核可，目前沒有替代通知機制（寄信步驟會被靜默略過）。
- 收件人清單是寫死的，尚未串接任何通訊錄／群組來源。
- 所有收件人收到完全相同的內容，沒有依角色或團隊做個人化呈現。
