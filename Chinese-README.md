# AI News Agent

一套 n8n 自動化系統，每日自動蒐集 AI 相關新聞，透過 LLM（Gemini）進行評分、分類與摘要，經過 Google Sheets 上的人工審核關卡後，於每週固定時間依分類彙整成 HTML 公告並以 Email 寄出。

## 運作原理

兩條 n8n workflow 彼此獨立，只透過共用的 Google Sheet 溝通，不會互相直接呼叫——因此可以各自獨立除錯、排程、或重跑，不會互相影響。

```
Workflow A（每日執行）                      Workflow B（每週執行）
──────────────────                        ───────────────────
11 個 RSS 新聞來源                          讀取「待審核」分頁中
   │                                       核可 = TRUE 且 已寄送 = FALSE 的項目
   ▼                                                │
合併來源 + 比對「已處理」歷史紀錄去重                ▼
   │                                       依 ChromeOS 專區 + 四大分類分組，
   ▼                                       各區塊依分數排序組成 HTML 公告內容
篩選近7天內、依新到舊排序                              │
   │                                                ▼
限制筆數上限（20 則）                                  透過 SMTP 寄送
   │                                                │
   ▼                                                ▼
單次批次呼叫 Gemini                          將對應項目標記 已寄送 = TRUE
（一次請求評分、分類並摘要所有候選新聞，                │
 並以關鍵字判斷 ChromeOS 相關性）                      ▼
   │                                       清空整個「待審核」分頁
   ▼                                                │
分數達門檻 → 寫入「待審核」                            ▼
所有項目（不論分數）→ 寫入「已處理」            「已處理」僅保留近14天資料、
                                             依連結去重後重新寫回
```

- **Workflow A** 每天執行一次，蒐集候選新聞，用單次批次呼叫 LLM 完成評分、分類與摘要（不是逐則個別呼叫——這對免費層 API 的速率限制很重要），並依結果分別寫入 Google Sheet 的兩個分頁：`待審核`（分數達門檻、等待人工核可）與 `已處理`（不論分數高低，全部完整記錄，作為歷史紀錄與去重依據）。
- **Workflow B** 每週執行一次。人工需在此之前先到 `待審核` 分頁勾選要發送的項目（`核可`）。此 workflow 會依 ChromeOS 專區與四大分類（應用類／知識類／產業類／其他類）分區組成一封彙整信件並寄出、標記已寄送的項目，接著清空 `待審核` 供下週使用，並將 `已處理` 修剪成只保留近 14 天的滾動視窗。

## 檔案說明

| 檔案 | 說明 |
|---|---|
| `[AI News Agent] A-蒐集與評分.json` | 每日蒐集、去重、Gemini 批次評分／分類之 workflow |
| `[AI News Agent] B-審核後寄送.json` | 每週審核彙整、分區組信、寄信、清理 Sheets 之 workflow |

## 前置需求

- 一個 n8n 環境（Cloud 或自架皆可）
- 一個 Google 帳號，並準備：
  - 一份 Google Sheet，包含 `待審核` 與 `已處理` 兩個分頁（欄位結構見下方）
  - 在 n8n 裡設定好 Google Sheets OAuth2 憑證
- 一組 Gemini API Key（於 [aistudio.google.com/apikey](https://aistudio.google.com/apikey) 申請），在 n8n 裡設定為憑證使用
- 一個可用於寄信的 SMTP 帳號

## 設定步驟

### 1. 建立 Google Sheets

建立一份試算表，包含以下兩個分頁，並在第一列填入完全一致的欄位名稱：

**`待審核`**
```
標題 | 摘要 | 分類 | 分數 | 評分理由 | 來源連結 | 發布日期 | 核可 | 已寄送 | ChromeOS相關
```

**`已處理`**
```
標題 | 摘要 | 分類 | 分數 | 評分理由 | 來源連結 | 發布日期 | 處理時間 | ChromeOS相關
```

`核可`、`已寄送`、`ChromeOS相關` 建議設成核取方塊格式——**只套用在標題列即可**，不要對一大片空白範圍預先套用格式，否則之後新資料可能會被寫入到很後面的列，而不是緊接在現有資料之後。

### 2. 在 n8n 設定憑證

- **Google Sheets OAuth2**：`Credentials → New → Google Sheets OAuth2 API`，用擁有上述試算表的 Google 帳號完成授權。
- **SMTP**：`Credentials → New → SMTP`。若使用 Gmail，需先開啟兩步驟驗證，再申請一組[應用程式密碼](https://myaccount.google.com/apppasswords)——Host 填 `smtp.gmail.com`，Port `587`，開啟 STARTTLS。
- **Gemini API Key**：在 `Credentials → New` 裡建立 Google Gemini(PaLM) API 類型的憑證，貼上你的 API Key。Workflow A 的「Gemini 評分與摘要」節點（HTTP Request）已設定為使用此類型的憑證，匯入後需在該節點重新選取你剛建立的憑證。

### 3. 匯入兩個 workflow

把兩份 JSON 檔案匯入 n8n（可用 `Import from File`，或直接把 JSON 內容貼到空白畫布上）。

### 4. 替換所有 placeholder

兩份檔案裡凡是跟環境相關的資訊，都用 `{PLACEHOLDER}` 這種格式標記，需要逐一搜尋替換，並且**在每個 Google Sheets／SMTP／Gemini 節點裡重新選一次憑證**（因為佔位用的憑證 ID 對應不到任何真實憑證）：

| Placeholder | 出現位置 | 替換成 |
|---|---|---|
| `{YOUR_GOOGLE_SHEET_ID}` | 兩份 workflow 裡所有 Google Sheets 節點的 `documentId` | 試算表網址中的 ID（`.../d/`與`/edit`中間那一串） |
| `{GOOGLE_SPREADSHEETS_LINK}` | 兩份 workflow 裡所有 Google Sheets 節點的 `cachedResultUrl` | 僅供 n8n 介面顯示用的試算表網址，不影響實際執行，可填可不填 |
| `{SMTP_SENDER}` | Workflow B「SMTP寄送公告」節點 | 寄件信箱地址 |
| `{AD_RECIPIENTS}` | Workflow B「SMTP寄送公告」節點 | 收件人地址，多個收件人用逗號分隔 |

Gemini API Key 不再以 Header placeholder 的方式寫在節點裡，改為 n8n 憑證，設定方式見上方「在 n8n 設定憑證」。

### 5. 依自己團隊調整評分 Prompt

Prompt 內容寫在 Workflow A 的「組合批次請求」節點裡，目前是針對一般性的 PC／硬體研發團隊設計的，並已預先納入 ChromeOS、開源 AI 生態、中國大陸模型動態、AI 實務操作分享等主題——正式使用前，建議把部門背景、關鍵字範例、分類定義都改成符合自己團隊的實際情況，評分結果才會準確。ChromeOS 相關性判斷是用程式碼關鍵字比對（見「設計筆記」），不是透過 Prompt，如果要調整比對關鍵字，需直接修改該節點的程式碼。

要留意：這段 Prompt 每次執行都會被送往外部 LLM API，**避免把公司具名、合作夥伴清單等敏感內部資訊直接寫進去**。

### 6. 先測試再啟用

1. 手動執行一次 Workflow A（`Execute workflow`），確認 `待審核`、`已處理` 都有正確寫入資料，含 `ChromeOS相關` 欄位。
2. 在 `待審核` 分頁勾選一兩筆的 `核可`。
3. 手動執行一次 Workflow B，確認信件依分類分區塊寄出、`已寄送` 有被標記、`待審核` 有被清空（含標題列是否完整）、`已處理` 的清理與去重邏輯也正常運作。
4. 確認都沒問題後，把兩個 workflow 切換成 **Active**。

## 資料模型

### `待審核` 分頁

| 欄位 | 說明 |
|---|---|
| 標題 | LLM 生成的精簡標題 |
| 摘要 | 2-3 句話的摘要 |
| 分類 | LLM 判斷的分類標籤，四選一：應用類／知識類／產業類／其他類 |
| 分數 | 0-10 的整數相關性分數 |
| 評分理由 | 一句話說明評分理由 |
| 來源連結 | 新聞原始連結——同時也是去重／比對的依據欄位 |
| 發布日期 | 新聞原始發布時間 |
| 核可 | 核取方塊，人工核可是否納入本週公告 |
| 已寄送 | 核取方塊，寄送成功後系統自動標記 |
| ChromeOS相關 | 核取方塊，由程式碼以關鍵字（ChromeOS／Chrome OS／Chromebook）判斷是否相關，獨立於「分類」欄位，作為 Workflow B 區分 ChromeOS 專區之依據 |

### `已處理` 分頁

前七個欄位跟 `待審核` 相同，少了 `核可`／`已寄送`，多了：

| 欄位 | 說明 |
|---|---|
| 處理時間 | 系統實際執行評分的時間戳記（不同於新聞本身的發布時間） |

另外也保留 `ChromeOS相關` 欄位。`已處理` 會記錄**所有**經過評分的項目，不論分數高低，一方面避免同一則新聞被重複處理，另一方面也讓人工有機會複查是否有被 LLM 低估、值得撈回來的新聞。

## 新聞來源

目前採用 11 個 RSS 來源，全部彙整進同一個合併節點，共用同一組「近 7 天篩選 → 20 則上限 → 批次評分」流程，沒有個別配額：

| 類型 | 來源 |
|---|---|
| 官方部落格 | OpenAI Blog、Google Chromebooks Blog |
| Google News 搜尋（公司/關鍵字限定） | Anthropic News、Google News AI（廣泛 AI 關鍵字） |
| Google News 搜尋（站台限定） | TechOrange、Inside、鉅亨網 cnyes |
| Google News 搜尋（主題關鍵字） | 開源AI生態（LangChain／AutoGen／CrewAI 等）、中國AI模型動態（DeepSeek／通義千問／Kimi／豆包等）、AI實操教學分享、邊緣裝置AI整合（ChromeOS／embedded／wearable 等） |

## 評分機制

每則新聞由單一批次 LLM 呼叫（所有候選新聞打包成一次請求，而非逐則呼叫，詳見「設計筆記」）依以下四個維度給出 0-10 的整數分數：

- **internal_relevance**（部門相關性）——與團隊技術棧、工作內容的相關程度
- **efficiency_value**（效率提升價值）——是否有助於提升個人／團隊生產力
- **must_know**（產業重要性）——即使不直接相關，是否仍屬產業重大消息
- **learning_value**（學習價值）——即使跟工作沒有直接關聯，是否仍值得閱讀學習

建議的分數區間：9-10 分（所有人都該讀）、7-8 分（高度相關）、5-6 分（特定職能相關）、3-4 分（相關性有限）、0-2 分（不建議）。目前收錄進 `待審核` 的預設門檻是 7 分。

Prompt 額外包含一條排除規則：純活動宣傳、品牌行銷、業配內容即使關鍵字命中相關主題，也會被要求明顯降低分數，避免「有提到 AI／Edge AI 但本質是廠商宣傳稿」的新聞被誤判為高分。

### 分類（四大類）

LLM 會將每則新聞歸類到以下四類其中之一，作為 Workflow B 组信時的分區依據：

- **應用類**——讀者可直接拿來操作、安裝、使用的東西（工具、平台、開源專案、操作教學）
- **知識類**——偏向閱讀性質的技術新知、研究內容
- **產業類**——產業趨勢、市場動態、重大商業消息
- **其他類**——僅在完全不屬於以上三類時使用

### ChromeOS／Chromebook 標記

與四大分類欄位彼此獨立，**不是**由 Gemini 語意判斷，而是在送進 Gemini 評分之前，由「組合批次請求」節點以程式碼關鍵字比對標題與內容（比對 `ChromeOS`／`Chrome OS`／`Chromebook`，不分大小寫），算出布林值後隨結果一併寫入 Sheet。Workflow B 依此欄位獨立列出 ChromeOS 專區。

## HTML 公告格式（Workflow B）

「組裝HTML公告內容」節點依下列規則組成公告：

- **ChromeOS 專區**：獨立列出所有 `ChromeOS相關` 為真的已核可項目，不論其分類為何，不會再出現在下方四大分類區塊中。
- **應用類／知識類／產業類／其他類**：各自成一區塊，依分數排序（同分時以發布日期較新者優先）顯示前 3 則；不足 3 則就顯示實際筆數，一則都沒有則整區塊省略。
- 各區塊 Top 3 之外，若還有其餘 ≥7 分的已核可項目，列在下方作為「其他高分項目」，只顯示標題與星等，不顯示摘要；若無則此段落省略。
- 排版：各區塊標題（18px）依分類上色（ChromeOS＝琥珀色、應用類＝藍色、知識類＝紅色、產業類＝綠色、其他類＝中性灰），色塊只出現在標題列，內文清單為透明背景；上榜項目標題 14px 並保留摘要，額外列出項目標題 11px、不顯示摘要。
- 相關度以星等（★）呈現，分數每高於 5 分多 1 星（上限 5 星）；標題本身即為可點擊超連結，不另外顯示「閱讀全文」連結。

## 設計筆記

幾個不看程式碼不容易看出來的設計決策，之後要修改時可以參考：

- **批次評分，而非逐則呼叫**：每次執行時，所有候選新聞會打包成單一個 Prompt，一次呼叫 Gemini 完成評分，而不是每則新聞各呼叫一次。這樣可以讓每日 API 用量遠低於免費層的速率限制。
- **模型使用別名，不釘死版本號**：Gemini 模型用的是 `-latest` 這種別名，而不是特定日期版本，因為 Google 會不定期讓特定版本的模型下架、不再開放新用戶使用。
- **去重機制依賴 `已處理`，不是 `待審核`**：因為 `待審核` 每週都會被完全清空，長期的去重歷史全部仰賴 `已處理`。14 天的保留期刻意設得比 7 天的新聞蒐集範圍更寬，多留一週緩衝空間。去重只比對「來源連結」是否完全相同，不同站台報導同一事件但連結不同的情況不會被系統自動辨識為重複。
- **RSS 來源本身不一定有時間限制**：部分 RSS 來源會回傳全站歷史文章，而不是只回傳近期項目，所以 workflow 自己另外做了一層依發布日期的篩選，不能完全仰賴來源本身。
- **ChromeOS 標記用關鍵字而非 LLM 判斷**：因為是明確的產品名稱，不需要語意判斷，用程式碼比對既準確又不會增加 Gemini 的輸出負擔或出錯風險。
- **核可篩選改用程式碼判斷，不用 Google Sheets 節點內建篩選**：Workflow B 的「讀取已核可項目」節點改成讀取全部資料，後面接一個「篩選已核可未寄送」Code 節點做型態寬鬆的布林判斷（同時處理布林值與各種大小寫字串），取代原本內建的字串比對篩選——因為 Google Sheets 核取方塊實際回傳的型態不保證一致，字串比對容易漏篩。

## 目前限制

- 11 個 RSS 來源共用同一組「近 7 天篩選 → 20 則上限」流程，沒有個別配額；來源蒐集量差異大時，涵蓋範圍廣泛的來源可能會佔滿候選名額，排擠掉範圍較窄的主題來源。
- 分數為整數，沒有同分時的細分機制；Workflow B 组信時同分則以發布日期較新者優先排序，但 Workflow A 候選評分階段本身沒有處理同分的邏輯。
- 跨天或跨站報導同一事件的重複新聞，目前沒有自動去重機制（去重僅依「來源連結」是否完全相同判斷），需仰賴人工審核時自行判斷是否重複。
- 「取回待標記項目」節點目前讀取的是「讀取已核可項目」的原始輸出，而非「篩選已核可未寄送」篩選後的結果；正常流程下兩者應該一致，但如果待審核分頁中曾經留有未清空的舊資料，建議留意「已寄送」欄位是否有非預期的標記範圍。
- 「清空待審核表單」使用的是「清除指定範圍列（從第 2 列開始）」而非整表清除＋重寫表頭，不同 n8n 版本對此參數的處理方式不完全一致，建議每次清空後人工確認標題列是否完整。
- 當週若沒有任何項目被核可，目前沒有替代通知機制（寄信步驟會被靜默略過）。
- 收件人清單是寫死的，尚未串接任何通訊錄／群組來源。
- 所有收件人收到完全相同的內容，沒有依角色或團隊做個人化呈現。
